import React from "react"
import App from "../App"

// when button is clicked, need to call Country on the specific country the button was clicked for
// or, change filter to only 1 country?



const Button = (props) => {
    return(
        <button>
            show
        </button>
    )
  }

// const handleClick = (event) => {
//     App.setNewFilter(props.value)
// }

export default Button